# DO NOT edit this file by hand, qutebrowser will overwrite it.
# Instead, create a config.py - see :help for details.

config_version: 1
global:
  bindings.commands:
    normal:
      <Space>+n: back
      H: tab-move -
      L: tab-move +
      gi: hint -m number inputs
      h: tab-prev
      l: tab-next
      t: :set-cmd-text -s :open -t
      th: null
      tl: null
  url.default_page: https://www.google.se/
  url.incdec_segments:
  - path
  - query
  url.searchengines:
    DEFAULT: https://www.google.se/search?q={}
  url.start_pages: https://www.google.se/
